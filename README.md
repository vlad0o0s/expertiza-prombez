# Экспертиза Промбезопасность

Профессиональный корпоративный сайт для компании АНО ЭПЦ "ТОП ЭКСПЕРТ", специализирующейся на экспертизе промышленной безопасности. Сайт построен на PHP с использованием компонентной архитектуры, современного дизайна и адаптивной верстки.

## 📋 О проекте

Сайт представляет собой информационный портал с возможностью отправки заявок через форму обратной связи. Все основные секции сайта реализованы в виде переиспользуемых компонентов, что упрощает поддержку и расширение функциональности.

### Основные возможности:

- ✅ Компонентная архитектура для легкой поддержки и расширения
- ✅ Адаптивный дизайн (desktop, tablet и mobile версии)
- ✅ Отправка форм через AJAX без перезагрузки страницы
- ✅ SMTP отправка писем через Beget
- ✅ Модальные окна для уведомлений
- ✅ Анимации при скролле
- ✅ Слайдер отзывов (Swiper.js)
- ✅ FAQ аккордеон
- ✅ Маска ввода телефона (IMask.js)
- ✅ SEO оптимизация с Schema.org разметкой
- ✅ Модальное меню навигации
- ✅ Страницы шаблонов экспертиз с динамическим контентом

## 🏗️ Структура проекта

```
expertiza-prombez.ru/
├── assets/                    # Статические ресурсы
│   ├── css/                   # Стили
│   │   ├── main.css          # Основные стили и CSS переменные
│   │   ├── header-footer.css # Стили шапки и подвала
│   │   ├── index.css         # Общие стили для главной страницы
│   │   └── messages.css      # Стили для сообщений
│   ├── js/                    # JavaScript файлы
│   │   ├── main.js           # Основной JS (меню, формы, плавная прокрутка)
│   │   ├── index-animations.js # Анимации при скролле
│   │   ├── index-forms.js    # Обработка форм (устарел, используется в компонентах)
│   │   ├── index-reviews.js  # Инициализация слайдера отзывов (устарел)
│   │   └── index-faq.js      # Обработка FAQ (устарел)
│   └── images/                # Изображения и иконки
│       ├── logo.png          # Логотип компании
│       ├── mainImg.png       # Главное изображение hero секции
│       ├── our_services1-8.png # Изображения услуг
│       ├── our1-4.svg        # Иконки преимуществ
│       └── ...               # Другие изображения
│
├── components/                 # Компоненты сайта (модульная архитектура)
│   ├── about-us/              # Секция "О нас" со статистикой
│   │   ├── component.php
│   │   ├── component.css
│   │   └── component.js       # Анимация счетчиков статистики
│   ├── articles/              # Список статей
│   │   ├── component.php
│   │   └── component.css
│   ├── articles-filters/      # Фильтры для статей
│   │   ├── component.php
│   │   └── component.css
│   ├── articles-list/         # Список статей (альтернативный)
│   │   ├── component.php
│   │   └── component.css
│   ├── breadcrumbs/           # Хлебные крошки навигации
│   │   ├── component.php
│   │   └── component.css
│   ├── categories-expertiza/  # Категории экспертиз
│   │   ├── component.php
│   │   └── component.css
│   ├── clients/               # Секция клиентов
│   │   ├── component.php
│   │   ├── component.css
│   │   └── component.js
│   ├── commercial-proposal-form/ # Форма запроса КП
│   │   ├── component.php
│   │   └── component.css
│   ├── company-banner/        # Баннер о компании
│   │   ├── component.php
│   │   └── component.css
│   ├── contact-form/          # Универсальная форма контактов
│   │   ├── component.php
│   │   ├── component.css
│   │   └── component.js       # AJAX отправка, валидация
│   ├── contacts/              # Контакты и форма обратного звонка
│   │   ├── component.php
│   │   ├── component.css
│   │   └── component.js       # AJAX отправка формы, маска телефона
│   ├── expertiza-template/    # Шаблон страницы экспертизы
│   │   ├── component.css      # Стили для hero, who-needs, features
│   │   └── component.js       # Логика для страницы экспертизы
│   ├── faq/                   # FAQ аккордеон
│   │   ├── component.php
│   │   ├── component.css
│   │   └── component.js       # Логика аккордеона
│   ├── footer/                # Подвал сайта
│   │   ├── component.php
│   │   └── component.css
│   ├── header/                # Шапка сайта
│   │   ├── component.php
│   │   └── component.css
│   ├── hero/                  # Главный баннер главной страницы
│   │   ├── component.php
│   │   └── component.css
│   ├── info-cards/            # Информационные карточки
│   │   ├── component.php
│   │   └── component.css
│   ├── other-services-section/ # Секция "Другие экспертизы"
│   │   ├── component.php
│   │   ├── component.css
│   │   └── component.js
│   ├── pagination/            # Пагинация
│   │   ├── component.php
│   │   └── component.css
│   ├── responsibility-section/ # Секция об ответственности
│   │   ├── component.php
│   │   └── component.css
│   ├── reviews/               # Слайдер отзывов
│   │   ├── component.php
│   │   ├── component.css
│   │   └── component.js       # Инициализация Swiper
│   ├── services/              # Секция услуг (8 услуг)
│   │   ├── component.php
│   │   └── component.css
│   ├── stages-section/        # Секция этапов работы
│   │   ├── component.php
│   │   └── component.css
│   ├── warning-section/       # Секция предупреждений
│   │   ├── component.php
│   │   └── component.css
│   └── why-choose-us/         # Секция "Почему нас выбирают"
│       ├── component.php
│       └── component.css
│
├── config/                     # Конфигурационные файлы
│   ├── config.php             # Основная конфигурация (SMTP, константы)
│   ├── mail.php               # Класс MailSender для отправки писем
│   ├── seo.php                # SEO настройки для страниц
│   ├── schema.php             # Функции генерации Schema.org разметки
│   └── modal-menu.php         # Конфигурация модального меню
│
├── includes/                   # Вспомогательные файлы
│   ├── header.php             # Шапка сайта (шаблон)
│   ├── footer.php             # Подвал сайта (шаблон)
│   ├── menu.php               # Меню навигации
│   ├── component-loader.php   # Функция загрузки компонентов
│   └── modal-menu-render.php  # Рендеринг модального меню
│
├── pages/                      # Отдельные страницы сайта
│   ├── expertiza-prombezopasnosti.php  # Страница всех экспертиз
│   ├── expertiza-template.php  # Шаблон страницы конкретной экспертизы
│   └── zapros-kommercheskogo-predlozheniya.php # Страница запроса КП
├── index.php                   # Главная страница
├── contacts.php                # Страница контактов
├── stati.php                   # Страница статей
├── composer.json               # Зависимости Composer (PHPMailer)
├── package.json                # Зависимости npm (ESLint)
├── sendmail.php                # Обработчик отправки писем
├── site.webmanifest            # Манифест для PWA
└── README.md                   # Документация
```

## 🚀 Установка и настройка

### Требования

- PHP 7.4 или выше
- Composer
- Веб-сервер (Apache/Nginx) или OpenServer
- Доступ к SMTP серверу для отправки писем

### Установка

1. **Клонируйте репозиторий или скопируйте файлы проекта**

2. **Установите зависимости Composer:**
```bash
composer install
```

Это установит:
- PHPMailer (для отправки писем через SMTP)

3. **Настройте конфигурацию:**

Откройте `config/config.php` и укажите:
- Настройки SMTP (хост, порт, логин, пароль)
- Email получателя заявок
- Данные организации для Schema.org

Пример настройки SMTP Beget:
```php
define('SMTP_HOST', 'smtp.beget.com');
define('SMTP_USERNAME', 'your_email@domain.ru');
define('SMTP_PASSWORD', 'your_password');
define('SMTP_PORT', 465);
define('SMTP_FROM_EMAIL', 'your_email@domain.ru');
define('SMTP_TO_EMAIL', 'recipient@gmail.com');
```

4. **Настройте веб-сервер:**

Убедитесь, что корневая директория проекта указывает на папку проекта, и включен mod_rewrite (для Apache).

## 📦 Компонентная архитектура

Проект использует модульную компонентную архитектуру. Каждый компонент находится в своей папке и содержит:

- `component.php` - HTML структура компонента
- `component.css` - CSS стили компонента
- `component.js` - JavaScript функциональность (опционально)

### Использование компонентов

Компоненты загружаются через функцию `load_component()` из `includes/component-loader.php`:

```php
<?php
require_once 'includes/component-loader.php';

// Загрузка компонента без данных
load_component('hero');
load_component('services');

// Загрузка с передачей данных
load_component('breadcrumbs', [
    'items' => [
        ['title' => 'Главная', 'url' => '/'],
        ['title' => 'Текущая страница', 'url' => null]
    ]
]);

load_component('contact-form', [
    'form_id' => 'contact-form',
    'name_id' => 'name',
    'phone_id' => 'phone',
    'consent_id' => 'consent'
]);
?>
```

**Как работает `load_component()`:**
1. Автоматически добавляет CSS файл в глобальный массив `$additional_css`
2. Автоматически добавляет JS файл в глобальный массив `$additional_js`
3. Использует `extract($data)` для передачи данных в компонент
4. Включает `component.php` через `include`

**ВАЖНО:** CSS и JS файлы компонентов подключаются в `header.php` и `footer.php` соответственно через массивы `$additional_css` и `$additional_js`.

### Создание нового компонента

1. Создайте папку в `components/` с именем компонента (kebab-case)
2. Создайте файлы:
   - `component.php` - HTML разметка (обязательно)
   - `component.css` - стили (опционально)
   - `component.js` - JavaScript (опционально)
3. Используйте компонент: `load_component('component-name')`

**Пример структуры компонента:**
```php
<!-- components/my-component/component.php -->
<section class="my-component">
    <div class="container">
        <?php if (isset($title)): ?>
            <h2><?php echo htmlspecialchars($title); ?></h2>
        <?php endif; ?>
        <!-- контент -->
    </div>
</section>
```

```css
/* components/my-component/component.css */
.my-component {
    padding: 40px 0;
}
```

### Особенности компонентов

**Компоненты с данными:**
- `breadcrumbs` - принимает массив `items`
- `contact-form` - принимает `form_id`, `name_id`, `phone_id`, `consent_id`
- `responsibility-section` - принимает `title`, `text`, `icon`
- `warning-section` - принимает `warning_primary`, `warning_secondary`, `contact_form_id`, `warning_image`, `background_image`
- `other-services-section` - принимает `title`, `services` (массив)
- `stages-section` - принимает `title`, `stages` (массив), `indicator_image`, `contact_form_id`
- `expertiza-template` - используется на страницах экспертиз, не загружается через `load_component()`, а подключается напрямую

## 🎨 Цветовая палитра и стили

### CSS переменные

Проект использует CSS переменные для цветов, определенные в `assets/css/main.css`:

```css
:root {
  --color-primary: #e60012;        /* Красный - основной акцент */
  --color-secondary: #91a2b8;      /* Серо-голубой - вторичный */
  --color-light-gray: #e6e6e6;     /* Светло-серый */
  --color-light-blue: #e9f1fc;     /* Светло-голубой фон */
  --color-white: #ffffff;           /* Белый */
  --color-dark-blue: #152333;       /* Темно-синий - основной текст */
  --color-dark-gray: #3c3c3c;       /* Темно-серый */
  --color-black: #0a0a0a;           /* Черный */
}
```

**ВАЖНО:** Всегда используйте CSS переменные вместо хардкода цветов:
```css
color: var(--color-primary);
background: var(--color-light-blue);
border-color: var(--color-dark-blue);
```

### Типографика

**Основной шрифт:** `Montserrat` (400, 500, 600, 700)
- Используется для заголовков и основного текста
- Загружается через Google Fonts с preload

**Дополнительный шрифт:** `Inter` (500)
- Используется для кнопок и специальных элементов

**Моноширинный:** `Martian Mono`
- Используется для счетчиков статистики

### Контейнеры

Проект использует три типа контейнеров:

1. **`.container`** - основной контейнер (max-width: 1200px, padding: 20px)
2. **`.container-fluid`** - широкий контейнер (max-width: 1280px, padding: 20px)
3. **`.container-full`** - полноширинный без отступов

**Особенность:** На страницах экспертиз используется кастомный контейнер:
```css
.container {
    max-width: 80rem; /* 1280px */
    padding: 0 100px;
}
```

### Адаптивные брейкпоинты

- **Desktop:** > 1024px
- **Tablet:** 769px - 1024px
- **Mobile:** ≤ 768px

**Паттерн медиа-запросов:**
```css
/* Планшет */
@media (min-width: 769px) and (max-width: 1024px) {
    /* стили */
}

/* Мобильная версия */
@media (max-width: 768px) {
    /* стили */
}
```

## 📱 Адаптивность

Сайт полностью адаптирован для мобильных устройств. Все компоненты имеют медиа-запросы для экранов шириной до 768px.

**Особенности мобильной версии:**
- Упрощенная навигация через модальное меню
- Оптимизированные размеры шрифтов
- Адаптированные изображения
- Вертикальное расположение элементов
- Специальные мобильные версии компонентов (где необходимо)

**Особенности адаптивности страниц экспертиз:**
- На мобильных используется `display: contents` для перестройки порядка элементов
- Элементы переупорядочиваются через CSS `order` свойство
- Контейнеры меняют padding с 100px на 10px на мобильных
- Изображения адаптируются по размеру и высоте

## 🔧 Основные функции

### 1. Форма обратного звонка

Форма с AJAX отправкой без перезагрузки страницы:
- Маска ввода телефона (+7)
- Валидация полей
- Модальное окно с результатом
- Отправка на email через SMTP

**Расположение:** `components/contacts/`

### 2. Слайдер отзывов

Слайдер на базе Swiper.js:
- 3 слайда видно одновременно на desktop
- Адаптивное количество на мобильных
- Навигация и пагинация

**Расположение:** `components/reviews/`

### 3. FAQ аккордеон

Интерактивный FAQ:
- Раскрывающиеся вопросы
- Анимация стрелок
- Один открытый элемент за раз

**Расположение:** `components/faq/`

### 4. Анимации при скролле

Плавные анимации появления элементов:
- Использует Intersection Observer API
- Класс `animate-on-scroll` для элементов
- Задержки через классы `delay-1`, `delay-2`, etc.

**Файл:** `assets/js/index-animations.js`

### 5. Анимация счетчиков

Анимированные счетчики статистики:
- Автоматический подсчет до целевого значения
- Активация при появлении в области видимости

**Расположение:** `components/about-us/component.js`

### 6. Модальное меню

Адаптивное модальное меню:
- Бургер-меню для мобильных
- Трехколоночная структура на desktop
- Аккордеон разделов

**Файлы:** `includes/modal-menu-render.php`, `config/modal-menu.php`

## 📧 Настройка отправки писем

### SMTP Beget

Проект настроен для работы с SMTP Beget:

```php
// config/config.php
define('SMTP_HOST', 'smtp.beget.com');
define('SMTP_PORT', 465); // SSL
define('SMTP_USERNAME', 'your_email@domain.ru');
define('SMTP_PASSWORD', 'your_password');
define('SMTP_TO_EMAIL', 'recipient@gmail.com');
```

### Использование MailSender

```php
require_once 'config/mail.php';

$mailSender = new MailSender();
$mailSender->send(
    'recipient@example.com',
    'Тема письма',
    '<h1>HTML содержимое</h1>',
    ['path/to/file.pdf'] // опциональные вложения
);
```

## 🎯 SEO оптимизация

### Schema.org разметка

Проект автоматически генерирует структурированные данные через `config/schema.php`:
- **Organization** - данные организации (генерируется на всех страницах)
- **WebSite** - для главной страницы
- **ContactPage** - для страниц контактов
- **AboutPage** - для страниц "О нас"
- **Service** - для страниц услуг
- **WebPage** - для обычных страниц

**Функции:**
- `generateSchemaOrg($schemaType, $seo)` - основная функция генерации
- `generateOrganizationSchema()` - генерация данных организации

**Использование:** Автоматически вызывается в `includes/header.php` на основе `$seo['schema_type']`

### SEO настройки страниц

Каждая страница имеет SEO конфигурацию в `config/seo.php`:

```php
'index' => [
    'title' => 'Заголовок страницы',
    'description' => 'Описание для поисковиков',
    'keywords' => 'ключевые, слова',
    'og_title' => 'Open Graph заголовок',
    'og_description' => 'Open Graph описание',
    'og_image' => SITE_URL . '/assets/images/og-image.jpg',
    'og_type' => 'website',
    'canonical' => SITE_URL . '/',
    'schema_type' => 'WebSite',
],
```

**Переопределение на странице:**
Можно переопределить `title` и `description` прямо на странице:
```php
$page_title = 'Кастомный заголовок';
$page_description = 'Кастомное описание';
```

**Доступные страницы в SEO конфиге:**
- `index` - главная
- `contacts` - контакты
- `expertiza-prombezopasnosti` - все экспертизы
- `stati` - статьи
- `zapros-kommercheskogo-predlozheniya` - запрос КП
- `expertiza-template` - шаблон экспертизы
- `default` - значения по умолчанию

## 📄 Создание новой страницы

### Базовая структура страницы

```php
<?php
require_once 'config/config.php';
require_once 'includes/component-loader.php';

// Указание ключа страницы для SEO (должен быть в config/seo.php)
$page_key = 'page-name';

// Подключение CSS/JS (опционально)
$additional_css = ['/assets/css/custom.css'];
$additional_js = [
    'https://cdn.jsdelivr.net/npm/imask@6.4.3/dist/imask.min.js',
    '/assets/js/custom.js'
];

include 'includes/header.php';
?>

<main>
    <!-- Используйте компоненты -->
    <?php load_component('breadcrumbs', [
        'items' => [
            ['title' => 'Главная', 'url' => '/'],
            ['title' => 'Новая страница', 'url' => null]
        ]
    ]); ?>
    
    <?php load_component('hero'); ?>
    
    <!-- Или собственный контент -->
    <section class="custom-section">
        <div class="container">
        <h1>Заголовок</h1>
        <p>Контент страницы</p>
        </div>
    </section>
</main>

<?php include 'includes/footer.php'; ?>
```

### Создание страницы шаблона экспертизы

Страницы экспертиз используют специальную структуру. Пример: `pages/expertiza-template.php`

**Особенности:**
1. Данные экспертизы хранятся в массиве `$expertiza_data`
2. Используется компонент `expertiza-template` (CSS/JS подключаются вручную)
3. Специальные компоненты: `responsibility-section`, `warning-section`, `other-services-section`, `stages-section`

**Структура данных:**
```php
$expertiza_data = [
    'title' => 'Название экспертизы',
    'category' => 'Категория Э3',
    'image' => '/assets/images/hero.png',
    'description' => 'Краткое описание',
    'full_text' => '<p>Полный HTML текст</p>',
    'features_text' => '<p>Особенности экспертизы</p>',
    'responsibility_text' => 'Текст об ответственности',
    'warning_primary' => 'Предупреждение 1',
    'warning_secondary' => 'Предупреждение 2',
    'other_services' => [/* массив других услуг */],
    'stages' => [/* массив этапов */]
];
```

### Добавление SEO для новой страницы

1. Откройте `config/seo.php`
2. Добавьте новый ключ:
```php
'page-name' => [
    'title' => 'Заголовок страницы',
    'description' => 'Описание для поисковиков',
    'keywords' => 'ключевые, слова',
    'og_title' => 'Open Graph заголовок',
    'og_description' => 'Open Graph описание',
    'og_image' => SITE_URL . '/assets/images/og-image.jpg',
    'og_type' => 'website',
    'canonical' => SITE_URL . '/page-url',
    'schema_type' => 'WebPage', // или 'Service', 'ContactPage', etc.
],
```
3. Используйте `$page_key = 'page-name'` на странице

## 🛠️ Технологии

### Backend
- **PHP 7.4+** - серверная логика
- **PHPMailer** - отправка писем через SMTP
- **Composer** - управление зависимостями

### Frontend
- **HTML5** - разметка
- **CSS3** - стилизация, Flexbox, Grid
- **JavaScript (ES6+)** - интерактивность
- **Swiper.js** - слайдеры (CDN)
- **IMask.js** - маски ввода (CDN)

### Библиотеки (CDN)
- **Swiper 11** - для слайдеров
- **IMask 6.4.3** - для масок ввода телефона
- **Google Fonts** - Montserrat, Inter

## 📝 Структура страниц

### Главная страница (`index.php`)

Состоит из следующих компонентов в порядке:

1. **Hero** - главный баннер с заголовком и кнопками
2. **Info Cards** - три информационные карточки
3. **Services** - 8 услуг компании (grid из карточек)
4. **Company Banner** - баннер с информацией о компании
5. **Why Choose Us** - преимущества компании (4 карточки)
6. **Contacts** - контакты и форма обратного звонка
7. **About Us** - информация о компании со статистикой (анимированные счетчики)
8. **Reviews** - слайдер отзывов клиентов (Swiper)
9. **FAQ** - часто задаваемые вопросы (аккордеон)

**Особенности:**
- Обработка формы обратного звонка происходит в `index.php` (AJAX)
- Подключаются дополнительные CSS/JS для анимаций
- Используется `$page_key = 'index'` для SEO

### Страница контактов (`contacts.php`)

Использует компонент `contacts` с формой обратной связи.

### Страница экспертиз (`pages/expertiza-prombezopasnosti.php`)

Список всех категорий экспертиз.

### Страница шаблона экспертизы (`pages/expertiza-template.php`)

**Структура:**
1. **Breadcrumbs** - хлебные крошки
2. **Hero Section** (встроенный) - изображение, заголовок, категория, описание, кнопки
3. **Who Needs Section** (встроенный) - кому нужна экспертиза
4. **Features Section** (встроенный) - особенности ЭПБ
5. **Responsibility Section** - ответственность за непроведение
6. **Warning Section** - предупреждения
7. **Other Services Section** - другие экспертизы (карусель)
8. **Contact Form** - форма заказа услуги
9. **Stages Section** - этапы проведения ЭПБ
10. **Reviews** - отзывы
11. **FAQ** - вопросы

**Особенности:**
- Данные хранятся в массиве `$expertiza_data`
- CSS/JS для `expertiza-template` подключаются вручную
- Используются специальные компоненты с передачей данных

## 🔐 Безопасность

### Меры безопасности

- **Валидация данных:** Все пользовательские данные валидируются на сервере
- **XSS защита:** Использование `htmlspecialchars()` для вывода данных
- **Санитизация:** Очистка входных данных форм через `trim()`
- **SMTP:** Безопасная отправка писем через SMTP с SSL
- **Error logging:** Ошибки логируются в PHP error_log

### Рекомендации

- Добавить CSRF токены для форм
- Добавить rate limiting для форм
- Валидация email и телефона на стороне сервера
- Использовать prepared statements при работе с БД (если будет добавлена)

## 📞 Контактная информация

**Компания:** АНО ЭПЦ "ТОП ЭКСПЕРТ"  
**Телефон:** +7 495 127 09-35  
**Email:** info@te-g.ru  
**Адрес:** Гамсоновский пер., 2, стр. 2, этаж 2, офис 211

## 🐛 Отладка

### Логи ошибок

Ошибки отправки писем логируются в:
- PHP error_log (настроен в `config/mail.php`)
- Консоль браузера (для JavaScript ошибок)

### Проверка SMTP

Если письма не отправляются:
1. Проверьте настройки SMTP в `config/config.php`
2. Убедитесь, что порт 465 открыт (для SSL)
3. Проверьте логи PHP
4. Убедитесь, что PHPMailer установлен через Composer

## 📚 Дополнительная информация

### Шрифты

Проект использует Google Fonts:
- **Montserrat** (400, 500, 600, 700) - основные заголовки и текст
- **Inter** (500) - кнопки и специальные элементы
- **Martian Mono** - для счетчиков статистики (если используется)

**Загрузка:** Шрифты загружаются с preconnect и preload для оптимизации

### Анимации

Все анимации плавные и профессиональные, подходящие для корпоративного сайта:

**Анимации при скролле:**
- Класс `.animate-on-scroll` - базовая анимация появления
- Класс `.fade-in` - простое появление
- Классы `.delay-1` до `.delay-8` - задержки для последовательного появления
- Используется Intersection Observer API (`assets/js/index-animations.js`)

**Hover эффекты:**
- Плавные переходы на кнопках (`transition: all 0.3s ease`)
- Тени при наведении на карточки
- Изменение цвета текста и фона

**Специальные анимации:**
- Анимация счетчиков в `about-us` компоненте
- Анимация аккордеона в FAQ
- Плавное открытие модальных окон

### Производительность

- Оптимизированные изображения (PNG, SVG)
- Ленивая загрузка шрифтов (media="print" onload)
- CDN для внешних библиотек (Swiper, IMask)
- Минификация CSS/JS (рекомендуется для продакшена)

### Конфигурация проекта

**Основные константы в `config/config.php`:**
- `SITE_NAME` - название сайта
- `SITE_URL` - URL сайта
- `SMTP_*` - настройки SMTP
- `ORG_*` - данные организации для Schema.org

**Текущие настройки SMTP:**
- Хост: `smtp.beget.com`
- Порт: `465` (SSL)
- От: `smtp@realmdigital.ru`
- Кому: `mail.vlad0o0s@gmail.com`

### Структура форм

**Форма обратного звонка (`components/contacts/`):**
- Поля: имя, телефон, комментарий (опционально)
- Валидация: обязательные поля, согласие на обработку данных
- AJAX отправка без перезагрузки
- Маска телефона через IMask.js

**Универсальная форма контактов (`components/contact-form/`):**
- Принимает параметры через `load_component()`
- Настраиваемые ID полей
- AJAX отправка
- Модальное окно с результатом

### Работа с изображениями

Все изображения находятся в `assets/images/`:
- Логотипы и иконки: SVG
- Фотографии и иллюстрации: PNG
- Изображения услуг: `our_services1.png` - `our_services8.png`
- Слайдер: `slider_0000_slider3.png` и т.д.

**Пути к изображениям:** Всегда используйте абсолютные пути от корня: `/assets/images/image.png`

## 📄 Лицензия

Проект разработан для коммерческого использования.

## 👨‍💻 Разработка

### Паттерны и соглашения

**При разработке новых компонентов:**
1. Следуйте существующей структуре компонентов
2. Используйте CSS переменные для цветов (никогда не хардкодьте цвета)
3. Добавляйте адаптивные стили для мобильных (max-width: 768px) и планшетов (769px-1024px)
4. Тестируйте на разных разрешениях экрана
5. Используйте семантический HTML
6. Оптимизируйте изображения
7. Используйте `htmlspecialchars()` для вывода пользовательских данных
8. Добавляйте комментарии в CSS для секций

**Структура CSS файла компонента:**
```css
/* ============================================
   Название компонента
   ============================================ */

/* Основные стили */
.component-name {
    /* стили */
}

/* Адаптивность для планшета */
@media (min-width: 769px) and (max-width: 1024px) {
    /* стили */
}

/* Мобильная версия */
@media (max-width: 768px) {
    /* стили */
}
```

**Структура PHP компонента:**
```php
<?php
// Комментарий о назначении компонента
// Если компонент принимает данные, проверяйте их наличие
?>

<section class="component-name">
    <div class="container">
        <?php if (isset($title)): ?>
            <h2><?php echo htmlspecialchars($title); ?></h2>
        <?php endif; ?>
        <!-- контент -->
    </div>
</section>
```

**Структура JS файла компонента:**
```javascript
// Инициализация при загрузке DOM
document.addEventListener('DOMContentLoaded', function() {
    // код компонента
});
```

### Работа с компонентом expertiza-template

**ВАЖНО:** Этот компонент НЕ загружается через `load_component()`, а используется напрямую на страницах экспертиз.

**CSS:** Подключается вручную в `$additional_css`
**JS:** Подключается вручную в `$additional_js`

**Структура CSS:**
- `.hero-section` - основная секция с изображением и описанием
- `.who-needs-section` - секция "Кому нужна экспертиза"
- `.features-section` - особенности ЭПБ
- Адаптивность с перестройкой порядка элементов на мобильных

**Особенности мобильной версии:**
- Используется `display: contents` для перестройки структуры
- Элементы переупорядочиваются через CSS `order`
- Контейнеры меняют padding с 100px на 10px

### Отладка

**Проверка подключения компонентов:**
- Убедитесь, что `component-loader.php` подключен
- Проверьте, что CSS/JS файлы существуют
- Проверьте консоль браузера на ошибки

**Проверка форм:**
- Откройте DevTools → Network → XHR
- Отправьте форму и проверьте запрос
- Проверьте ответ сервера (JSON)

**Проверка SMTP:**
- Проверьте настройки в `config/config.php`
- Проверьте логи PHP (error_log)
- Убедитесь, что PHPMailer установлен

### Полезные команды

```bash
# Установка зависимостей Composer
composer install

# Проверка синтаксиса PHP (если установлен)
php -l filename.php

# Линтинг JS (если настроен ESLint)
npm run lint
```

---

## 📋 Чеклист для новых функций

При добавлении новой функциональности:

- [ ] Создан компонент в `components/`
- [ ] Добавлены CSS стили с адаптивностью
- [ ] Добавлен JS (если нужен)
- [ ] Используются CSS переменные для цветов
- [ ] Добавлена валидация данных (если форма)
- [ ] Используется `htmlspecialchars()` для вывода
- [ ] Протестировано на desktop, tablet, mobile
- [ ] Добавлены комментарии в код
- [ ] Обновлен README (если нужно)

---

**Версия:** 1.0  
**Дата обновления:** 2025
**Последний анализ:** Полный анализ структуры, стилей и компонентов
